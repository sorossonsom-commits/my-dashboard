<!DOCTYPE html>
<html lang="th">
  <head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0, maximum-scale=1.0, user-scalable=no">
    <title>Dashboard Center - Future UI</title>
    <link href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.0.0/css/all.min.css" rel="stylesheet">
    <link href="https://fonts.googleapis.com/css2?family=Chakra+Petch:wght@400;600;700&display=swap" rel="stylesheet">
    <style>
      :root {
        --neon-blue: #00f3ff;
        --neon-yellow: #facc15;
        --bg-base: #070b14;
        --panel-bg: rgba(13, 22, 45, 0.6);
      }
      
      body, html {
        margin: 0;
        padding: 0;
        width: 100%;
        height: 100dvh; /* ใช้ dvh เพื่อให้พอดีกับหน้าจอมือถือเป๊ะๆ (นับรวมแถบ URL) */
        overflow: hidden; /* ล็อคหน้าจอไม่ให้เลื่อนขึ้นลง */
        font-family: 'Chakra Petch', sans-serif;
        background-color: var(--bg-base);
        color: #fff;
      }

      /* พื้นหลังสไตล์โลกอนาคต (ตาราง Grid แบบโฮโลแกรม) */
      .cyber-bg {
        position: absolute;
        top: 0; left: 0; right: 0; bottom: 0;
        background:
          linear-gradient(rgba(0, 243, 255, 0.05) 1px, transparent 1px),
          linear-gradient(90deg, rgba(0, 243, 255, 0.05) 1px, transparent 1px);
        background-size: 40px 40px;
        z-index: -2;
      }

      /* แสงเงารอบๆ พื้นหลัง */
      .cyber-bg::after {
        content: '';
        position: absolute;
        inset: 0;
        background: radial-gradient(circle at center, transparent 0%, var(--bg-base) 90%);
        z-index: -1;
      }

      .dashboard-wrapper {
        display: flex;
        flex-direction: column;
        height: 100%;
        padding: 3vh 5vw;
        box-sizing: border-box;
        align-items: center;
        justify-content: space-evenly; /* กระจายช่องว่างให้สมดุลบนทุกหน้าจอ */
      }

      header {
        text-align: center;
        flex-shrink: 0;
        z-index: 10;
      }

      h1 {
        color: var(--neon-blue);
        /* ปรับขนาดฟอนต์อัตโนมัติตามขนาดจอ */
        font-size: clamp(1.8rem, 4vw + 1rem, 3.5rem); 
        margin: 0 0 1vh 0;
        text-transform: uppercase;
        text-shadow: 0 0 15px rgba(0, 243, 255, 0.6);
        letter-spacing: 2px;
      }

      .subtitle {
        color: #8da4c8;
        font-size: clamp(1rem, 2vw + 0.5rem, 1.5rem);
        margin: 0;
        letter-spacing: 2px;
      }

      .grid {
        display: flex;
        flex-direction: row; /* แนวนอนสำหรับจอใหญ่ */
        gap: 3vw;
        width: 100%;
        max-width: 1200px;
        height: 65vh; /* บังคับความสูงของการ์ดไม่ให้ล้นจอ */
        justify-content: center;
        z-index: 10;
      }

      .card {
        flex: 1;
        max-width: 500px;
        background: var(--panel-bg);
        border: 1px solid rgba(0, 243, 255, 0.15);
        border-radius: 16px;
        padding: 3vh 2vw;
        display: flex;
        flex-direction: column;
        align-items: center;
        justify-content: center;
        text-decoration: none;
        color: #fff;
        backdrop-filter: blur(12px); /* เอฟเฟกต์กระจก */
        box-shadow: 0 10px 30px rgba(0,0,0,0.5), inset 0 0 20px rgba(0, 243, 255, 0.05);
        transition: all 0.3s cubic-bezier(0.25, 0.8, 0.25, 1);
        position: relative;
      }

      /* ดีไซน์มุมกรอบสไตล์ Sci-Fi */
      .card::before, .card::after {
        content: ''; position: absolute; width: 20px; height: 20px; border: 2px solid transparent; transition: all 0.3s;
      }
      .card::before { top: 15px; left: 15px; border-top-color: var(--neon-blue); border-left-color: var(--neon-blue); }
      .card::after { bottom: 15px; right: 15px; border-bottom-color: var(--neon-blue); border-right-color: var(--neon-blue); }

      .card-1:hover { 
        border-color: var(--neon-blue); 
        box-shadow: 0 0 30px rgba(0, 243, 255, 0.2), inset 0 0 20px rgba(0, 243, 255, 0.1); 
        transform: translateY(-5px); 
      }
      
      .card-2 { border-color: rgba(250, 204, 21, 0.15); }
      .card-2::before { border-top-color: var(--neon-yellow); border-left-color: var(--neon-yellow); }
      .card-2::after { border-bottom-color: var(--neon-yellow); border-right-color: var(--neon-yellow); }
      .card-2:hover { 
        border-color: var(--neon-yellow); 
        box-shadow: 0 0 30px rgba(250, 204, 21, 0.2), inset 0 0 20px rgba(250, 204, 21, 0.1); 
        transform: translateY(-5px); 
      }

      .icon {
        font-size: clamp(3.5rem, 8vh, 6rem);
        margin-bottom: 2vh;
        filter: drop-shadow(0 0 12px currentColor); /* ไอคอนเรืองแสง */
      }
      .card-1 .icon { color: var(--neon-blue); }
      .card-2 .icon { color: var(--neon-yellow); }

      .card h3 {
        margin: 0 0 1vh 0;
        font-size: clamp(1.3rem, 3vw + 0.5rem, 2rem);
        letter-spacing: 1px;
        text-align: center;
      }

      .card p {
        color: #a0b2d8;
        margin: 0 0 3vh 0;
        font-size: clamp(0.9rem, 1.5vw + 0.5rem, 1.2rem);
        text-align: center;
      }

      /* ปุ่มสไตล์ Cyberpunk (มีรอยตัดมุม) */
      .btn {
        padding: 1.5vh 3vw;
        font-size: clamp(0.9rem, 1.5vw + 0.5rem, 1.1rem);
        font-weight: 600;
        text-transform: uppercase;
        display: flex;
        align-items: center;
        gap: 10px;
        clip-path: polygon(10px 0, 100% 0, 100% calc(100% - 10px), calc(100% - 10px) 100%, 0 100%, 0 10px);
        transition: all 0.3s ease;
      }

      .card-1 .btn { 
        background: rgba(0, 243, 255, 0.05); 
        color: var(--neon-blue); 
        border: 1px solid var(--neon-blue); 
      }
      .card-1:hover .btn { 
        background: var(--neon-blue); 
        color: #000; 
        box-shadow: 0 0 20px var(--neon-blue); 
      }

      .card-2 .btn { 
        background: rgba(250, 204, 21, 0.05); 
        color: var(--neon-yellow); 
        border: 1px solid var(--neon-yellow); 
      }
      .card-2:hover .btn { 
        background: var(--neon-yellow); 
        color: #000; 
        box-shadow: 0 0 20px var(--neon-yellow); 
      }

      /* การตั้งค่าสำหรับหน้าจอมือถือโดยเฉพาะ */
      @media (max-width: 768px) {
        .dashboard-wrapper {
          padding: 2vh 4vw;
        }
        .grid {
          flex-direction: column; /* เรียงการ์ดบน-ล่าง */
          height: 75vh; 
          gap: 2vh;
        }
        .card {
          padding: 2vh 4vw;
          border-radius: 12px;
        }
        .icon {
          margin-bottom: 1vh;
        }
        .card p {
          margin: 0 0 2vh 0;
        }
        .card::before, .card::after {
          width: 15px; height: 15px;
        }
        .btn {
          padding: 1.2vh 4vw;
        }
      }
      
      /* รองรับมือถือจอเล็กมาก (เช่น iPhone SE) */
      @media (max-height: 650px) {
        .icon { font-size: 3rem; }
        .card h3 { font-size: 1.1rem; }
        .card p { font-size: 0.8rem; margin: 0 0 1.5vh 0; }
        .btn { padding: 1vh 3vw; font-size: 0.8rem; }
      }
    </style>
  </head>
  <body>

    <div class="cyber-bg"></div>

    <div class="dashboard-wrapper">
      <header>
        <h1>Battery Monitoring System</h1>
        <div class="subtitle">ฝ่ายปฏิบัติการเขตนครหลวง</div>
      </header>

      <div class="grid">
        <a href="https://script.google.com/macros/s/AKfycbwEoSjCH4x7BAgGAp8Akm_1x54yvvvLVlyN44onQwonw_0uEuutgdmUPwMKswc0HzhM/exec" target="_blank" rel="noopener noreferrer" class="card card-1">
          <div class="icon"><i class="fas fa-battery-full"></i></div>
          <h3>BATTERY MONITOR 125 V.</h3>
          <p>กองบำรุงรักษาสถานีไฟฟ้าแรงสูง</p>
          <div class="btn">เข้าดูข้อมูล <i class="fas fa-chevron-right"></i></div>
        </a>

        <a href="https://script.google.com/macros/s/AKfycbybr4Gh356bvITe0PR_dAGVurU1czBf1Zd0v5c2dQsV9ijG2GiZh2IXryGt96W-cBS1/exec" target="_blank" rel="noopener noreferrer" class="card card-2">
          <div class="icon"><i class="fas fa-battery-three-quarters"></i></div>
          <h3>BATTERY MONITOR 48 V.</h3>
          <p>กองบำรุงรักษาระบบสื่อสาร</p>
          <div class="btn">เข้าดูข้อมูล <i class="fas fa-chevron-right"></i></div>
        </a>
      </div>
    </div>

  </body>
</html>